[project]
name = "fabulor"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# Headless: tests drive Player's pure state machine directly (no mpv, no QApplication).
addopts = "-q"
