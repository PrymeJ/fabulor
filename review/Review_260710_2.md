# REVIEW — Standing invariant compliance sweep

> _Audited against commit `b0a90e4` (HEAD, `main`). All file:line citations reflect that tree._

Date: 2026-07-10
**Audit only — no fix applied.** Full-codebase check of 18 standing invariants, independent of any
recent-feature work. Each item verified against the current code with a file:line reference.

---

## Verdict

**All 18 invariants PASS. No failures. No escalations.** 88/88 tests green (`pytest tests/`, 1.69s).

Every load-bearing rule in the checklist holds in the current tree. Where an invariant is a
"single-implementation" rule (theme entry, compute+apply entry), the one legitimate implementation
was confirmed and no replicating call site was found. Where it is a "never use X" rule (native
chapter nav, hard deletes, book_events↔sessions JOIN, broad transparent QWidget), no violating usage
exists.

---

## Per-invariant results

### 1. `chapter_list` + epsilon used, never `self.player.chapter` for display/nav — PASS
The `chapter` property (getter/setter) is defined at
[player.py:721-734](src/fabulor/player.py#L721-L734). The only *read* of it outside its own
definition is the sanctioned `apply_smart_rewind` clamp-to-chapter-start at
[player.py:1034](src/fabulor/player.py#L1034) (`curr_idx = self.chapter`), which the rules explicitly
permit (native getter tracks playback position regardless of how the seek was issued). The setter
([player.py:731-733](src/fabulor/player.py#L731-L733)) is never called for navigation. Chapter
display derives from a `_chapter_list` walk with `_CHAPTER_WALK_TOLERANCE`
([app.py:1943-1946](src/fabulor/app.py#L1943-L1946)); chapter nav goes through
`activate_chapter_index`/`seek_async` with `_chapter_seek_offset()`
([player.py:833-840](src/fabulor/player.py#L833-L840)). The two remaining `self.chapter = idx`
textual hits ([player.py:849](src/fabulor/player.py#L849),
[chapter_list.py:377](src/fabulor/ui/chapter_list.py#L377)) are comments.

### 2. MPV init untouched since last known-good state — PASS
`_ensure_mpv` block ([player.py:127-140](src/fabulor/player.py#L127-L140)) is byte-identical to the
last known-good state. `git log -L 127,141:player.py` shows its most recent change is the sanctioned
`audio_client_name='fabulor'` addition (`99b9bc4`) — nothing has touched it since. `locale.setlocale(locale.LC_NUMERIC, "C")` ([player.py:129](src/fabulor/player.py#L129)) and all constructor
args (`vo='null', ao='pulse', vid=False, ytdl=False, keep_open='always', audio_client_name='fabulor'`)
present and in order.

### 3. Animation guard intact in `_sync_chapter_ui` — PASS
[app.py:1923-1976](src/fabulor/app.py#L1923-L1976). All guards present:
`mp3_seek_reload_pending` early-return (1924), `self._switch.flow_pending_chapter` guard (1933),
`self.player.is_seeking` guard (1939), `_CHAPTER_WALK_TOLERANCE` walk (1945), and the flow-animation
`chap_animating` guard on `setValue` (1958-1960, applied at 1970).

### 4. `_apply_stylesheets()` is sole theme entry point — PASS
Every theme apply routes through `ThemeManager._apply_stylesheets`
([theme_manager.py:663+](src/fabulor/ui/theme_manager.py#L663)); `mw.setStyleSheet(get_base_stylesheet(...))`
lives inside it ([theme_manager.py:684](src/fabulor/ui/theme_manager.py#L684)). No competing global
`main_window.setStyleSheet(get_*)` exists. The two other `self.setStyleSheet(get_*)` calls
([tag_manager.py:721](src/fabulor/ui/tag_manager.py#L721),
[cover_panel.py:194](src/fabulor/ui/cover_panel.py#L194)) are per-component stylesheets on the
widget's own instance — the documented per-component architecture, not a global entry point.

### 5. `_add_row_safely`: `setVisible(False)` before `insertWidget` preserved — PASS
[stats_panel.py:3186-3189](src/fabulor/ui/stats_panel.py#L3186-L3189):
`widget.setVisible(False)` (3187) → `layout.insertWidget(...)` (3188) → `widget.setVisible(True)` (3189).
Order preserved.

### 6. `keep_open='always'` preserved — PASS
[player.py:131](src/fabulor/player.py#L131), in the MPV constructor.

### 7. `book_ready` gating (`ungate_play` / `_on_playlist_resolved`) unchanged — PASS
Three `book_ready.emit()` sites, all correctly partitioned:
- [player.py:428](src/fabulor/player.py#L428) (`_on_playlist_resolved`, gate-already-lifted branch) —
  gated by `if self._virtual_timeline is not None:` (VT only).
- [player.py:445](src/fabulor/player.py#L445) (`ungate_play`) — same VT-only gate.
- [player.py:508](src/fabulor/player.py#L508) (`_on_file_loaded`) — in the `else` of
  `if self._virtual_timeline is not None:` (non-VT only); the VT branch emits `file_switched` (506)
  instead. The two paths are mutually exclusive and never converge.

`_on_file_ready` connects **only** to `book_ready`
([app.py:364](src/fabulor/app.py#L364)), never to `file_loaded` — the anti-feedback-loop rule holds.

### 8. `_on_pause_test` still gated by `_is_vt_file_switch` — PASS
[player.py:220-228](src/fabulor/player.py#L220-L228): on `value` (paused) it returns early if
`self._is_vt_file_switch` (223-224) before the EOF `_advance_or_finish` check.

### 9. Cached values still cleared in `load_book` — PASS
[player.py:400-404](src/fabulor/player.py#L400-L404): `_cached_time_pos = None`,
`_cached_duration = None`, `_seek_target = None` — plus the full VT/chapter state reset
(387-399). Matches the "Duration race corollary" and the "asymmetric-clear" rules.

### 10. `terminate()` sequence unchanged — PASS
[player.py:826-831](src/fabulor/player.py#L826-L831): store ref (828) → clear `self.instance` (829)
→ `terminate()` (830) → `wait_for_shutdown()` (831). Exact order.

### 11. `apply_current_state()` sole compute+apply entry; no replicating call site — PASS
The `compute_library_state()` + `apply_library_state(state)` pair exists once, inside
`apply_current_state` itself ([library_controller.py:251-252](src/fabulor/library_controller.py#L251-L252)).
Every other site calls `apply_current_state()` (library_controller.py:99, 151, 257; app.py:906, 1379);
`_check_library_status` delegates to it ([library_controller.py:255-258](src/fabulor/library_controller.py#L255-L258)).
No call site inlines the two-liner.

### 12. `_update_cover_art_scaling` uses `COVER_AREA_HEIGHT=280`, not `cover_art_label.height()` — PASS
`COVER_AREA_HEIGHT = 280` ([ui_helpers.py:25](src/fabulor/ui/ui_helpers.py#L25)).
`_update_cover_art_scaling` sets `target_h = COVER_AREA_HEIGHT`
([app.py:2404](src/fabulor/app.py#L2404)); `cover_art_label.height()` is not read. (`target_w` uses
`cover_art_label.width()` — permitted; the rule constrains height only.)

### 13. No JOIN of `book_events` against `listening_sessions` anywhere — PASS
The only direct `JOIN book_events` ([db.py:467](src/fabulor/db.py#L467), `get_finished_book_data`)
joins to `books`, with no `listening_sessions` in the query — no cartesian fan-out against session
aggregation. The two session-aggregating queries that need a finished flag
(`get_daily_book_breakdown` [db.py:632](src/fabulor/db.py#L632), `get_books_listened_in_period`
[db.py:866](src/fabulor/db.py#L866)) use the mandated correlated scalar subquery
`(SELECT MAX(CASE WHEN be.event_type='finished' ...) FROM book_events be WHERE be.book_id = b.id)`.
Every other `SUM(...listened_seconds...)` query (db.py:719, 732, 743, 773, 823) touches no
`book_events`.

### 14. `get_book_count()` vs `get_visible_book_count()` usage correct at every call site — PASS
- `compute_library_state`'s `has_indexed_books` uses `get_visible_book_count()`
  ([library_controller.py:167](src/fabulor/library_controller.py#L167)) — the one place the rule
  mandates the fenced count.
- All `get_book_count()` sites are legitimate "any rows exist / sidebar-reachability" checks, per the
  documented G/A/S-shortcut and drag-area behavior: `_open_tags_shortcut`/`_open_stats_shortcut`/
  `_open_settings_shortcut` ([app.py:2162, 2177, 2184](src/fabulor/app.py#L2162)) mirror the sidebar's
  own right-click-open guard; `_on_drag_area_pressed` ([app.py:2462, 2473](src/fabulor/app.py#L2462))
  is the empty-state left-click short-circuit and the "only forward right-click when books indexed"
  guard. None is a library-visibility decision that should be fenced.

### 15. No hard deletes from `books` table — PASS
`grep "DELETE FROM books\b"` returns nothing. `remove_scan_location` soft-deletes via
`UPDATE books SET is_deleted = 1` ([db.py:264](src/fabulor/db.py#L264)); `restore_books_under_path`
un-soft-deletes via `UPDATE ... is_deleted = 0` ([db.py:250](src/fabulor/db.py#L250)). Only
`is_deleted`/`is_excluded`/`is_missing` flags are used.

### 16. No broad `QWidget { background: transparent; }` in panel stylesheets — PASS
No bare/unscoped `QWidget { background: transparent }` selector exists. The three `background:
transparent` rules ([themes.py:3512, 3804, 3810](src/fabulor/themes.py#L3512)) are all **scoped
descendant** selectors — `QTabWidget QWidget` and `QWidget#stats_time_tab QWidget` — the intentional,
bounded transparency-inheritance rules for tab-widget children, not a global leak.

### 17. `audio_client_name='fabulor'` still present in MPV constructor — PASS
[player.py:132](src/fabulor/player.py#L132).

### 18. `_set_tag_color` still doesn't call `refresh()` — PASS
[tag_manager.py:479-486](src/fabulor/ui/tag_manager.py#L479-L486): calls `db.set_tag_color`,
`_show_reserved("none")`, `_update_detail_dot`, `_update_list_dot` (targeted in-place dot updates),
and `tag_changed.emit()` — no `refresh()`. The `self.refresh()` at
[tag_manager.py:548](src/fabulor/ui/tag_manager.py#L548) belongs to `_show_list` (the back-to-list
navigation), a different method. The `tag_changed` signal drives only
`stats_panel._on_tag_changed` ([main_window_builders.py:581](src/fabulor/ui/main_window_builders.py#L581)),
not the tag manager's own list rebuild.

---

## Test status

`pytest tests/ -q` → **88 passed** (1.69s).
