# REVIEW PASS 4 — Theme System & Stylesheet Entry Points

> _Audited against commit `381d18a` (working tree at audit time; the audit's own source edits to player.py/book_detail_panel.py landed later in `c1ac7b0`). file:line citations reflect that tree and will drift as code changes._

Date: 2026-06-12
Scope: [src/fabulor/ui/theme_manager.py](src/fabulor/ui/theme_manager.py) (read in full) + [src/fabulor/themes.py](src/fabulor/themes.py) (stylesheet-function structure + targeted greps) + app.py setStyleSheet audit.
Outcome legend: **(a)** correct · **(b)** violating · **(c)** symbol not found.

| # | Check | Outcome | Severity |
|---|-------|---------|----------|
| 1 | No full-app `main_window.setStyleSheet()` | (a) | — |
| 2 | `_apply_stylesheets` passes `suppress_bg_image=getattr(mw,'_bg_suppressed',False)` | (a) | — |
| 3 | `_apply_stylesheets` ends with `mw._set_chapter_ui_active(mw._chapter_ui_active)` | (a) | — |
| 4 | `_do_fade_with_slider_animation` skips chapter slider when `_chapter_ui_active` False | (a) | — |
| 5 | No `background-image: none` override in any stylesheet function | (a) | — |
| 6 | Per-component stylesheets set only in `_apply_stylesheets` | (a)* | low (sanctioned exceptions) |
| 7 | No `visual_area.setStyleSheet` outside `_set_bg_suppressed` | (a) | — |

No violations. Detail below.

---

## 1. No full-app `main_window.setStyleSheet()` — (a) correct

The single `mw.setStyleSheet(...)` is [theme_manager.py:552](src/fabulor/ui/theme_manager.py#L552):
```python
mw.setStyleSheet(get_base_stylesheet(theme_name))
```
This is the **documented** `main_window` → `get_base_stylesheet()` mapping (bg, tooltips, chapter_dropdown, undo overlay) — a per-component scoped stylesheet, **not** a full-app one. The CLAUDE.md rule prohibits passing a *full-app* stylesheet to `main_window.setStyleSheet()`; `get_base_stylesheet` is the sanctioned per-component sheet for the window chrome. Grep for `self.setStyleSheet`/`main_window.setStyleSheet` in app.py returns nothing. ✅

## 2. `suppress_bg_image` threaded through — (a) correct

[theme_manager.py:558-560](src/fabulor/ui/theme_manager.py#L558-L560):
```python
mw.content_container.setStyleSheet(
    get_player_stylesheet(theme_name, suppress_bg_image=getattr(mw, '_bg_suppressed', False))
)
```
Exact form required by the rule — a theme change in the no-book/empty state keeps the bg image stripped. ✅

## 3. `_apply_stylesheets` ends with `_set_chapter_ui_active` reapply — (a) correct

[theme_manager.py:581-582](src/fabulor/ui/theme_manager.py#L581-L582):
```python
if hasattr(mw, '_set_chapter_ui_active'):
    mw._set_chapter_ui_active(mw._chapter_ui_active)
```
Last statement in the method. Reapplies the correct chapter-UI cursor/stylesheet/transparency state after the repolish wave from the seven `setStyleSheet` calls clears instance stylesheets. ✅

## 4. `_do_fade_with_slider_animation` skips inactive chapter slider — (a) correct

[theme_manager.py:472-477](src/fabulor/ui/theme_manager.py#L472-L477):
```python
for attr in ('progress_slider', 'chapter_progress_slider'):
    s = getattr(mw, attr, None)
    if s is not None and s.isVisible():
        if attr == 'chapter_progress_slider' and not mw._chapter_ui_active:
            continue   # inactive (ghosted) — keep covered by overlay, no punch-through
        sliders.append(s)
```
The guard skips `chapter_progress_slider` when `_chapter_ui_active` is False, so it is not added to `sliders` and therefore not punched out of the overlay mask ([theme_manager.py:505-508](src/fabulor/ui/theme_manager.py#L505-L508)) — preventing the full-opacity flash through the punch-through window. The guard form differs cosmetically from CLAUDE.md's quote (it lives in the slider-collection loop rather than a generic attr sweep) but is the same predicate and the same effect. ✅

## 5. No `background-image: none` override in stylesheet functions — (a) correct

All nine stylesheet functions present in themes.py:
- `get_base_stylesheet` [themes.py:2290](src/fabulor/themes.py#L2290)
- `get_title_bar_stylesheet` [themes.py:2438](src/fabulor/themes.py#L2438)
- `get_player_stylesheet` [themes.py:2467](src/fabulor/themes.py#L2467)
- `get_library_stylesheet` [themes.py:2552](src/fabulor/themes.py#L2552)
- `get_settings_stylesheet` [themes.py:2728](src/fabulor/themes.py#L2728)
- `get_stats_stylesheet` [themes.py:3001](src/fabulor/themes.py#L3001)
- `get_tags_stylesheet` [themes.py:3333](src/fabulor/themes.py#L3333)
- `get_sidebar_stylesheet` [themes.py:3507](src/fabulor/themes.py#L3507)
- `get_cover_panel_stylesheet` [themes.py:3552](src/fabulor/themes.py#L3552)

`background-image` appears at only two lines: [themes.py:2476](src/fabulor/themes.py#L2476) (a **comment** in `get_player_stylesheet` explaining why `none` doesn't work) and [themes.py:2486](src/fabulor/themes.py#L2486) (`background-image: url({bg_path})` — the actual image rule, conditionally omitted when `suppress_bg_image=True`). No `background-image: none` rule exists in any function. Suppression is by *omission at generation time*, as mandated. ✅

## 6. Per-component stylesheets set only in `_apply_stylesheets` — (a)* correct, sanctioned exceptions

Grep for `*.setStyleSheet` on the five named widgets (content_container, library_panel, stats_panel, tags_panel, sidebar) outside theme_manager.py returns exactly one hit:

- **[app.py:961](src/fabulor/app.py#L961)** `self.content_container.setStyleSheet(get_player_stylesheet(theme_name, suppress_bg_image=suppressed))` — inside `_set_bg_suppressed` ([app.py:937](src/fabulor/app.py#L937)), which CLAUDE.md explicitly designates as **"the sole authority"** for the bg-image suppression re-apply. This is a documented, sanctioned exception, not a violation. It regenerates the *same* per-component `get_player_stylesheet`, just with the suppression flag — it does not introduce a competing full-app or off-pattern sheet.

Two additional notes (not violations):
- `get_cover_panel_stylesheet` is applied in [cover_panel.py:190-194](src/fabulor/ui/cover_panel.py#L190-L194) via `self.setStyleSheet(...)` — the CoverPanel owns its own sheet and is **not** routed through `_apply_stylesheets` (cover_panel is absent from the `_apply_stylesheets` widget list). This matches the architecture: the 9th stylesheet function has a dedicated owner outside the theme-manager dispatch. Consistent, not a defect.
- `library_panel` also calls `mw.library_panel.update_progress_bar_theme()` ([theme_manager.py:565](src/fabulor/ui/theme_manager.py#L565)) and `chapter_list_widget.update_theme(...)` ([theme_manager.py:568](src/fabulor/ui/theme_manager.py#L568)) — these are theme-propagation hooks, not raw `setStyleSheet` calls, all inside `_apply_stylesheets`. Fine.

Severity low — purely to flag that the "only in `_apply_stylesheets`" rule has one documented carve-out (`_set_bg_suppressed`) plus the independent CoverPanel owner. Both are by design. ✅

## 7. No `visual_area.setStyleSheet` outside `_set_bg_suppressed` — (a) correct

Grep for `visual_area.setStyleSheet` across src/fabulor returns **nothing**. `visual_area` is never given an instance stylesheet anywhere — suppression is done by regenerating `content_container`'s sheet (the ancestor that paints `#visual_area`) inside `_set_bg_suppressed`, plus `setAutoFillBackground(not suppressed)` at [app.py:953](src/fabulor/app.py#L953). No child override is attempted, exactly as the rule requires. ✅
